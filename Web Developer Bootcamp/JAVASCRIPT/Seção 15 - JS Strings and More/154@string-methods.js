// METHODS //
// these are an actons we can make with a string, using the dot (.)
// we can uppercase, lowercase, duplicate or combine strings, for example
// sintax:
thing.method()

let msg = "hello";
"msg"
'hello'

let upMsg = msg.toUpperCase()

"upMsg"
'HELLO'

// TRIM
// is going to trim off any white space at the beginning of a string or at the end
let text = "    hey!    "

"text"
'    hey!    '

text.trim()
'hey!'

// we can mix the methods
"text.trim().toUpperCase()"
'HEY!'