// ARRAY METHODS // 

const array1 = ['a', 'b', 'c'];
const array2 = ['d', 'e', 'f'];

// CONCAT
// is used to concat arrays and create a new third array
const array3 = array1.concat(array2);

// INDEXOF
// is used to find which is the indexOf some array's value
// it's the esiest way to tell if some element is in array or not
array2.indexOf('e') // 1

