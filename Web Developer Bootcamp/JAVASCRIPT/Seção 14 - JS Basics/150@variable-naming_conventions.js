// in the name of a variable, we have some hard rules by JS to follow

// HARD RULES //
// can contain unicode letters, $, _, and digits (0-9), but may not start with a digit.

let user = a; //correct
let user1 = a; //correct
let _user = a; //correct
// let 1user = a; //wrong

// CAMEL CASE //
// lowercase first letter | uppercase second name first letter | with out space
let userName = a;
let isGameOver = false;
